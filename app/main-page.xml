<Page 
xmlns="http://www.nativescript.org/tns.xsd" xmlns:maps="nativescript-google-maps-sdk" xmlns:FAB="nativescript-floatingactionbutton"
loaded="pageLoaded">
  <ActionBar title="CHEKIBREKI" class="action-bar"></ActionBar>
  <!-- Your UI components go here -->
<GridLayout rows="auto,*">
    <maps:mapView row="1" latitude="{{ latitude }}" longitude="{{ longitude }}" 
                                    zoom="{{ zoom }}" bearing="{{ bearing }}" 
                                    tilt="{{ tilt }}" padding="{{ padding }}" mapReady="onMapReady"  
                                markerSelect="onMarkerSelect" markerBeginDragging="onMarkerBeginDragging"
                                markerEndDragging="onMarkerEndDragging" markerDrag="onMarkerDrag"
                                cameraChanged="onCameraChanged" />
    <FAB:fab tap="fabTap"
                     row="1"
                     icon="res://ic_add_white"
                     rippleColor="#f1f1f1"
                     class="fab-button" />
  </GridLayout>
</Page>
